(jbuild_version 1)

(rule
 (with-stdout-to jbuild.inc.gen
  (run ocaml ${path:jbuild_gen.ml})))

(alias
 ((name jbuild)
  (action (diff jbuild.inc jbuild.inc.gen))))

(alias
 ((name runtest)
  (deps ((alias jbuild)))))

(include jbuild.inc)
